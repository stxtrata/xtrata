# SDK Archive

This folder contains completed planning/history documents that are kept for reference but are no longer part of the active SDK operating docs.

Use active docs in `docs/sdk/` for current implementation and release workflows.
