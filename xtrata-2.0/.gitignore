node_modules
.DS_Store
dist
coverage
.env
.env.local
.artifacts

# Wizard test wallet keys. Real mainnet private keys, never committed.
# scripts/wizard/.env.wizards.example holds the placeholders and IS committed.
.env.wizards
scripts/wizard/.env.wizards
scripts/wizard/KILL

# Thread runner journals. Local state about one operator's in-flight run:
# txids, inscription ids and what has been spent. No key material, by assertion.
scripts/wizard/.run-*.json


# FOLDERS

Archive/
!docs/sdk/archive/
!docs/sdk/archive/**

/AAA-Collection
# generated agent-one bundles (rebuilt by prebuild)
xtrata-agent-one/wizard/agent-one.js
xtrata-agent-one/wizard/agent-one-wallet.js
forever-twins/assets/xtrata-forever-twins.js

# Vite config bundling artifacts (auto-generated, safe to delete)
*.timestamp-*.mjs

# Cowork cloud-session recycle bin (bridge cannot delete files)
_to_delete/

# Mosaic simulator scratch state — scenarios are generated by seed-state.mjs
tools/mosaic-sim/state/
