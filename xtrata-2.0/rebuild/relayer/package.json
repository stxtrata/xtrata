{
  "name": "@xtrata/relayer",
  "version": "0.1.0",
  "description": "Rebuilt sponsor relayer for xtrata-drops-v3 (platform-agnostic core + Cloudflare adapter)",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@stacks/transactions": "^7.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.4.0",
    "typescript": "^5.6.0",
    "vitest": "^4.0.7"
  }
}
