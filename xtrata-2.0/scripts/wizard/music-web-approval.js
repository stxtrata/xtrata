(async()=>{const $=id=>document.getElementById(id),id=new URLSearchParams(location.search).get('id');async function call(path){const r=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});const s=await r.json();if(!r.ok)throw Error(s.error);return s;}
try{const p=await call('/web-review');if(p.kind==='approved'){$('details').textContent='Already approved. Return to the website.';return;}$('extension').textContent='Extension ID: '+p.extension;
$('details').textContent=p.kind==='pair'?'Allow https://xtrata.xyz to connect through this extension, create a local support wallet if you request one, and read its address, balance and recent payments. This does not authorise spending or withdrawals.':`Allow the Xtrata Music website to request automatic paid song starts for this session. Each costs ${p.fee} microSTX network fee plus 50 microSTX to the current song holder. No platform fee. This can spend your available balance. Stops when you turn support off, disconnect, or the website stops checking in for two minutes. No payment is sent by this approval itself.`;
$('consent').onchange=()=>{$('approve').disabled=!$('consent').checked;};$('approve').onclick=async()=>{$('approve').disabled=true;try{await call('/web-approve');$('consent').disabled=true;$('result').textContent='Approved. Return to the Xtrata Music lounge; it will update automatically.';}catch(e){$('result').textContent=e.message;$('approve').disabled=false;}};
}catch(e){$('result').textContent=e.message;}})();
