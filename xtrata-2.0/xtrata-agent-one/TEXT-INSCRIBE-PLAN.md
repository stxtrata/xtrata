# SUPERSEDED — see /INSCRIBE-PANEL-FILE-VS-TEXT-PLAN.md

This draft targeted the **Inscription Wizard**, which was the wrong surface. Text
inscription does not need the Wizard's deposit/escrow flow — it uses the embedded
`/inscribe` panel's existing **single-transaction** path.

The Wizard changes were reverted. The corrected plan (split the embedded on-site
inscription panel into File vs Text tabs) lives at the repo root:

**`INSCRIBE-PANEL-FILE-VS-TEXT-PLAN.md`**
