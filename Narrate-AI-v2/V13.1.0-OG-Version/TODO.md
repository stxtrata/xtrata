
Update so session budget auto sets to rounded up estimated $ value for the book after analyze and parse

Check the *** vs * * * voice separator logic as sometimes it has spaces sometimes it doesn't. Ideally we detect one of the other then update the switch token automatically if a different format is detected (only with or without spaces)

The save/load section isnt fully intuitive. We should have ability to name the project in the main window.

Could also update to allow various doc types to be imported rather than just copy paste. Accepting .txt, .docx etc would be good.

Clicking chapters and segments to view the selected text is great but need a way to zoom back out again to view full book text if desired.

Make certain warnings amber instead of red like when audio hasn't been generated yet it should be a warning not an error when user clicks the segment as they might just want to see the text.

It would be GREAT to implement some kind of local AI checks that listens to the books, compares script and identifies any unwanted sounds our glitches in the script as a final quality check of the generated audio.

